<odoo>
    <!-- Acción asociada al wizard -->
    <record id="action_generate_accounting_entries_menu" model="ir.actions.act_window">
        <field name="name">Buscar Movimientos Sin Valorización</field>
        <field name="res_model">generate.accounting.entries.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <!-- Menú para abrir el wizard -->
    <menuitem id="menu_generate_accounting_entries"
              name="Movimientos Sin Valorización"
              parent="stock.menu_stock_warehouse_mgmt"
              action="action_generate_accounting_entries_menu"
              sequence="10"/>
</odoo>
